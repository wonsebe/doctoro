<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="web.model.dao.ItemDao">    <!-- mapper start -->

    <!-- 카테고리명이 '강화 아이템'인 상품을 구매 시 아이템 테이블에 레코드 등록 -->
    <insert id="itemAdd" parameterType="web.model.dto.ItemDto">
        insert into items (item_use, uno, product_no) VALUES (#{item_use}, #{uno}, #{product_no});
    </insert>

    <!-- 내 아이템 목록 출력 -->
    <select id="itemPrint" resultType="web.model.dto.ItemDto" parameterType="int">
        select *
        from items inner join product on items.product_no = product.product_no
        where uno = #{loginUno} and item_use = 0;
    </select>

    <!-- 아이템 사용 클릭 시, 사용 여부 상태 변경 (0 -> 1) -->
    <update id="itemUse" parameterType="web.model.dto.UserDto">
        update items
        set item_use = 1
        where uno = #{uno} and product_no = #{product_no} and item_use = 0
        limit 1;
    </update>

</mapper>   <!-- mapper end -->